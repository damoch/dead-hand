namespace DeadHand.Commands.Enums
{
    internal enum CommandIdentifier
    {
        help, email, connect, shutdown, installTool, ecms, decoder, donis, status, weaponStatus, timeLeft,
        deadHand,
        checkRadio,
        weatherService,
        defrag
    }
}
